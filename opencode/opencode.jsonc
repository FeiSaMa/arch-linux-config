{
  "$schema": "https://opencode.ai/config.json",
  "username": "FeiSaMa",

  // ============================================================
  // DeepSeek 模型策略配置
  // 根据任务复杂度自动路由到不同模型以达到最大性价比
  // ============================================================

  // 默认使用 V4-Flash（平衡速度与能力）
  "model": "deepseek/deepseek-v4-flash",

  // 简单任务使用 v4-flash 非思考模式（与主模型同价，但免除 thinking 开销）
  "small_model": "deepseek/deepseek-v4-flash",

  // 默认使用 macro agent（意图精炼 + 分发总管）
  "default_agent": "macro",
  "shell": "/bin/zsh",
  "share": "disabled",
  "autoupdate": "notify",
  "formatter": false,
  "disabled_providers": ["openai", "anthropic", "google"],

  "provider": {
    "deepseek": {
      "options": {
        "timeout": 600000,
        "chunkTimeout": 30000
      },
      "models": {
        "deepseek-v4-flash": {
          "cost": {
            "input": 0.14,
            "output": 0.28
          }
        },
        "deepseek-v4-pro": {
          "cost": {
            "input": 0.435,
            "output": 0.87
          },
          // 开启 thinking 模式，利用 reasoning_effort: max 提升深度推理质量
          "options": {
            "thinking": {
              "type": "enabled",
              "reasoning_effort": "max"
            }
          }
        },
        "deepseek-chat": {
          // ⚠️ 将于 2026/07/24 弃用，当前映射到 deepseek-v4-flash 非思考模式
          "cost": {
            "input": 0.14,
            "output": 0.28
          }
        }
      }
    },
    // ============================================================
    // Fallback provider：当 DeepSeek API 不可用时的降级方案
    // 需自行设置环境变量 OPENAI_API_KEY
    // 取消注释下方整个块即可启用（同时注释掉 deepseek 块来做切换测试）
    // ============================================================
    // "openai": {
    //   "options": {
    //     "timeout": 300000,
    //     "chunkTimeout": 15000
    //   },
    //   "models": {
    //     "gpt-4o": {
    //       "cost": {
    //         "input": 5,
    //         "output": 15
    //       }
    //     },
    //     "gpt-4o-mini": {
    //       "cost": {
    //         "input": 0.15,
    //         "output": 0.6
    //       }
    //     }
    //   }
    // }
  },

  // ============================================================
  // 全局指令：适用于所有 Agent 的系统级提权规则
  // 提权规则统一在此处管理，各 Agent 不再单独定义
  // ============================================================
  "instructions": [
    "## 系统提权规则\n\n涉及系统级操作（修改 `/etc/`、重启服务、安装软件包等）时，必须正确选择提权方式：\n\n- **当前机器**（pam_faillock 已配置，sudo 失败 3 次锁定 10 分钟）：**必须使用 `pkexec`**\n- **新机器**（pam_faillock 未配置）：使用 `sudo`\n\n**检测方式**：`pkexec --version` 成功 → 用 `pkexec`；否则用 `sudo`。\n\n```bash\n# 示例：复制文件到 /etc/\npkexec cp file /etc/somewhere      # 当前机器\nsudo cp file /etc/somewhere         # 新机器\n```",
    "## 步骤日志记录规则\n\n你负责记录每步操作日志。在返回消息的**最末尾**附加以下代码块：\n\n```markdown\n## 执行日志\n\n### Step 1: [步骤简述]\n- **推理**: [思考过程]\n- **工具**: `tool_name` [关键参数]\n- **返回**: [关键内容摘要]\n- **结论**: [观察与判断]\n```\n\n**规则**：\n1. 每调用一次工具 = 一个 Step\n2. 禁止省略任何步骤\n3. 工具返回摘要控制在 3-5 行内，勿原文复制\n4. 日志附着在最终消息末尾，不单独发送\n5. 最后附加性能摘要小节",
    "## 写入前备份规则（Git 分级）\n\n子 Agent 在执行任何文件写入/编辑操作前，**必须先备份原文件**。session_id 由 macro 在分发 prompt 中指定（格式：`YYYYMMDD_HHmmss`）。\n\n### 备份策略（按文件状态分级）\n\n检测文件所在项目是否为 Git 仓库（`git rev-parse --show-toplevel`），若是则按文件状态分级处理：\n\n| 文件状态 | 备份方式 | I/O 开销 |\n|---------|---------|---------|\n| **Git 中且 HEAD 无修改**（`git diff --quiet HEAD -- <file>` 返回 0） | **零 I/O** — 记下 Git HEAD commit hash，无需物理备份 | 🔽 0% |\n| **Git 中有未提交修改**（`git diff --quiet HEAD -- <file>` 返回 1） | **保存 diff** — `git diff HEAD -- <file> > {backup_dir}/{path_flat}.diff` | 🔽 diff 大小 |\n| **Untracked 新文件**（`git ls-files --error-unmatch <file>` 失败但 `git rev-parse` 成功） | **全量拷贝** — `cp <file> {backup_dir}/{path_flat}.bak.{timestamp}` | 🔼 全量 |\n| **不在 Git 仓库中**（`git rev-parse` 失败） | **全量拷贝** — `cp <file> {backup_dir}/{path_flat}.bak.{timestamp}` | 🔼 全量 |\n\n### 备份目录\n\n`~/.cache/opencode-backup/{session_id}/`\n\n### Bash 参考\n\n```bash\nsession_id=\"{{session_id}}\"\nbackup_dir=\"$HOME/.cache/opencode-backup/$session_id\"\nmkdir -p \"$backup_dir\"\nfile=\"src/main.py\"\n# 检查是否在 git 仓库\nif git_root=$(git rev-parse --show-toplevel 2>/dev/null); then\n  cd \"$git_root\" || exit 1\n  # 检查文件是否在 git 中\n  if git ls-files --error-unmatch \"$file\" 2>/dev/null; then\n    # 文件在 git 中 — 检查是否有未提交修改\n    if git diff --quiet HEAD -- \"$file\" 2>/dev/null; then\n      echo \"$file: git HEAD is backup (zero I/O)\"\n      echo \"GIT_CLEAN:$file\" >> \"$backup_dir/manifest.txt\"\n    else\n      echo \"$file: saving diff\"\n      git diff HEAD -- \"$file\" > \"$backup_dir/${file//\\//_}.diff\"\n      echo \"DIFF:$file\" >> \"$backup_dir/manifest.txt\"\n    fi\n  else\n    echo \"$file: untracked, full copy\"\n    cp \"$file\" \"$backup_dir/${file//\\//_}.bak.$(date +%s)\"\n    echo \"FULL:$file\" >> \"$backup_dir/manifest.txt\"\n  fi\nelse\n  # 不在 git 仓库 — 全量拷贝\n  cp \"$file\" \"$backup_dir/${file//\\//_}.bak.$(date +%s)\"\n  echo \"FULL:$file\" >> \"$backup_dir/manifest.txt\"\nfi\n```\n\n此规则适用于所有拥有 `edit: allow` 权限的 Agent（build, code, create）。\n\n**重要**：子 Agent 在返回的「状态摘要」中，必须列出本次备份记录，格式：\n\n```markdown\n#### 状态摘要\n- **备份记录**:\n  - src/main.py (git:clean → 零 I/O)\n  - src/utils.ts (git:modified → diff saved)\n  - new_file.py (untracked → full copy)\n  - /etc/config.yml (no-git → full copy)\n```\n\n",
    "## 配置管理规则\n\n本机（`~/.config/opencode/`）是配置的权威源头，refs（`refs/arch-linux-config/`）是备份参考仓库。所有配置更改遵循：\n\n1. **参考仓库** — 软件配置更改参考 `refs/arch-linux-config`，硬件调优修改参考 `refs/arch-linux-config/hardware`\n2. **同步方向不可逆** — 所有配置更改**从本机同步到 refs**，禁止从 refs 提取覆盖本机，除非用户明确要求\n3. **任务完成后统一同步** — 整个任务完成、用户确认结果后，将本次任务涉及的所有文件**一次性同步**到 `refs/arch-linux-config/` 对应路径\n4. **统一推送远程** — refs 同步完成后，执行 `git add`、`git commit`、`git push` 统一推送到 GitHub\n5. **变更影响评估** — 配置同步属于低风险变更，跳过 verify",
  ],

  "references": {
    "arch-linux-config": {
      "path": "${HOME}/refs/arch-linux-config",
      "description": "Arch Linux system configuration docs — use for /etc/ config files, packages, services, dotfiles, and hardware tuning (see hardware/ subdirectory)"
    }
  },

  "skills": {
    "paths": ["${HOME}/.config/opencode/skills"]
  },

  "lsp": true,

  // ============================================================
  // 上下文压缩配置
  // ============================================================
  "compaction": {
    "auto": true,
    "prune": true,
    "reserved": 24000,
    "tail_turns": 20
  },

  // ============================================================
  // 工具输出限制：防止超长内容撑爆上下文
  // ============================================================
  "tool_output": {
    "max_lines": 200,
    "max_bytes": 8192
  },

  // ============================================================
  // Agent 体系：6 个专用 Agent 覆盖不同复杂度场景
  // 各 Agent 的完整定义已迁移至 ~/.config/opencode/agents/*.md
  // ============================================================

  // ============================================================
  // Agent 禁用：以下内建 Agent 被 macro 体系取代
  // ============================================================
  "agent": {
    "general": { "disable": true }  // 功能由 macro + 6 子 Agent 体系覆盖
  }
}
